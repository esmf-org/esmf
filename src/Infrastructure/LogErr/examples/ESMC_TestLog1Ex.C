
#include "ESMC.h"
#include "ESMC_LogErr.h"

int main() {
    ESMC_Log aLog,aLog2;
    int int_num=88;
    float real_num=3.5;
    char teststring[10]="xxxx";
    char ch ='s';
/*
    aLog.ESMC_LogInit();
    aLog2.ESMC_LogInit();
    aLog2.ESMC_LogOpenFile(ESMF_SINGLE_LOG_FILE,"aLog2.txt");
    aLog2.ESMC_LogNotVerbose();
    aLog.ESMC_LogNotHaltOnErr();
    aLog.ESMC_LogErr(ESMF_FATAL);
    aLog.ESMC_LogErrMsg(ESMF_FATAL,"oy");
    aLog2.ESMC_LogInfo("Hi there %d %c %f %s",int_num,ch,real_num,shep);
    aLog.ESMC_LogInfo("Hi there %d %c %f %s",int_num,ch,real_num,shep);
    aLog.ESMC_LogInfo("Hi there %f %s",real_num,shep);
    aLog.ESMC_LogInfo("Hi there %d %c ",int_num,ch);
    aLog.ESMC_LogWarnMsg(ESMF_FATAL,"oy this is a warning");
    aLog2.ESMC_LogVerbose();
    aLog2.ESMC_LogWarn(ESMF_FATAL);
    aLog2.ESMC_LogCloseFile();
    aLog.ESMC_LogHaltOnErr();
    aLog.ESMC_LogInfo("Hi there %d %c ",int_num,ch);
    */
}

