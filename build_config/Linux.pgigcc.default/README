Settings for Linux, using the commercial Portland Group PGI Fortran compiler
and the GNU C++ compiler (g++).

Requires the following environment settings:

ESMF_COMPILER pgigcc

ESMF is currently configured to use the PGI-provided version of LAPACK.
