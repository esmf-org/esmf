Settings for Linux, using the commercial Intel Fortran compiler "ifort" and
GNU's C++ compiler "g++". 
(This is also the correct configuration for an SGI Altix.)

Requires the following environment settings:

ESMF_COMPILER intelgcc

Defaults to 64 bit, can compile on 32-bit systems with:

ESMF_PREC 32

