#!/bin/sh
# this script expects the LLVM flang command (or a wrapper, e.g. mpif90)
# and returns the LLVM flang linker paths
$* -v $ESMF_DIR/scripts/hello.f90 2>&1 | grep "\-L" | awk 'BEGIN { RS=" "}; /^-L/ ' | xargs
rm -rf hello.o a.out*
